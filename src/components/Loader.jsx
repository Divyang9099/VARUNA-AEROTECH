import React from 'react';

const Loader = () => {
    return (
        <div className="loader flex items-center justify-center bg-white z-50">
            <div className="jimu-primary-loading"></div>
        </div>
    );
};

export default Loader;
